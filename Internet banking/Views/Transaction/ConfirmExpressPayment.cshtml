@using IB.Core.Application.ViewModels.Transaction
@model ConfirmTransactionViewModel

@{
    ViewData["Title"] = "Confirmar Pago Expreso";
}

<div class="container mt-5 w-50">
    <div class="card card-box-shadow">
        <div class="card-header bg-white">
            <h3>✅ Confirmar Pago Expreso</h3>
        </div>
        <div class="card-body">
            <p class="fs-5 text-center">Por favor, revisa los detalles de tu pago antes de confirmarlo.</p>

            <ul class="list-group mb-3">
                <li class="list-group-item"><strong>Cuenta de Origen:</strong> @Model.FromAccountNumber</li>
                <li class="list-group-item"><strong>Cuenta de Destino:</strong> @Model.ToAccountNumber</li>
                <li class="list-group-item"><strong>Destinatario:</strong> @Model.RecipientName</li>
                <li class="list-group-item"><strong>Monto a Pagar:</strong> @string.Format("{0:C}", Model.Amount)</li>
            </ul>

            <form method="post" asp-controller="Transaction" asp-action="ConfirmExpressPayment">
                <input type="hidden" asp-for="UserId" />
                <input type="hidden" asp-for="FromAccountNumber" />
                <input type="hidden" asp-for="ToAccountNumber" />
                <input type="hidden" asp-for="Amount" />

                <div class="text-center">
                    <button type="submit" class="btn btn-success">✅ Confirmar Pago</button>
                    <a class="btn btn-danger" asp-controller="Transaction" asp-action="CancelPaymentExpress">❌ Cancelar</a>
                </div>
            </form>
        </div>
    </div>
</div>
